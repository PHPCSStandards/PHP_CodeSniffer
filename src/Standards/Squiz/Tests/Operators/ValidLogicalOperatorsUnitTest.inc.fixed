<?php
if ($value === TRUE || $other === FALSE) {
}

if ($value === TRUE || $other === FALSE) {
}

if ($value === TRUE && $other === FALSE) {
}

if ($value === TRUE && $other === FALSE) {
}

if ($one === TRUE && ($two === TRUE || $three === TRUE)) {
}

if ($one === TRUE && ($two === TRUE || $three === TRUE)) {
}

if ($a ^ $b) {
}

if ($a xor $b) {
}

if ($a XOR $b) {
}

$c = $a and $b;
$c += $a and $b;
$c -= $a and $b;
$c *= $a and $b;
$c **= $a and $b;
$c /= $a and $b;
$c .= $a and $b;
$c %= $a and $b;
$c &= $a and $b;
$c |= $a and $b;
$c ^= $a and $b;
$c ?? $a and $b;
$c ??= $a and $b;
$c <<= $a and $b;
$c >>= $a and $b;

$c = ($a && $b);
$c = ($a and $c ?? ($c || $d));
$c = ($a && ($b ?? $c));
$foo = ($a and ($b ?? $c) ?? $d);
$foo = ($a && ($b ?? $c) || $d);
$foo = ($a and ($b ?? $c) or $d ?? $e);

$foo = ($a and $b || $c);
$foo = ($a or $b xor $c);
$foo = ($a && $b || $c);
print $a and $b;
yield $a and $b;
?>
